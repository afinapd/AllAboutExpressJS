class User {
    constructor(id, userName, userPassword, fullName, address) {
        this.id = id;
        this.userName = userName;
        this.userPassword = userPassword;
        this.fullName = fullName;
        this.address = address;
    }
}